// GraphQL Types
// This folder is for generated TypeScript types from GraphQL schema
// Run `pnpm generate` or `npm run generate` to generate types

export interface Scalars {
  ID: string;
  String: string;
  Boolean: boolean;
  Int: number;
  Float: number;
  DateTime: string;
  Decimal: string;
  JSON: string;
}
